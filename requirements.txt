# Core analysis
numpy>=1.24.0
pandas>=1.5.0
scipy>=1.10.0
statsmodels>=0.14.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Machine learning (for covariate matching evaluation)
scikit-learn>=1.2.0

# Data handling
pyarrow>=12.0.0  # for efficient CSV I/O

# Note: S-PrediXcan is run separately via command line (MetaXcan package)
# Note: Mahalanobis matching uses R matchit package (run separately)
